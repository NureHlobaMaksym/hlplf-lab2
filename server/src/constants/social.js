const POST_VISIBILITY = {
  PUBLIC: 'public',
  FRIENDS: 'friends'
};

const FRIEND_REQUEST_STATUS = {
  PENDING: 'pending',
  ACCEPTED: 'accepted',
  REJECTED: 'rejected'
};

const ALLOW_MESSAGES_FROM = {
  ALL: 'all',
  FRIENDS: 'friends'
};

module.exports = { POST_VISIBILITY, FRIEND_REQUEST_STATUS, ALLOW_MESSAGES_FROM };
